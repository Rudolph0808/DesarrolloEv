program llamadasError;

var
a : int;
texto : string;

int sumar(x : int, y : int) [
    {
        return x + y;
    }
];

void mostrar(msg : string) [
    {
        print(msg);
        return;
    }
];

int saludo(nombre : string) [
    {
        return nombre;
    }
];

main {
    a = sumar(1, 2, 3);
    a = sumar(5);
    a = mostrar("hola");
    a = noExiste(1);
    a = sumar(texto, 2);
}
end
